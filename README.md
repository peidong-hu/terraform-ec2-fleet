# Terraform scripts create spot/ondemand instances using ec2 fleet 

Run with a command like this for plan:

```
terraform plan -out=outputs
```

Run to apply the plan

```
terraform apply "outputs"
```

